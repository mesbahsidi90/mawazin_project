# متابعة تطوير موازين

المستودع المعتمد: `mesbahsidi90/mawazin_project`، الفرع `main`.

1. افحص رأس الفرع والتعديلات قبل العمل؛ لا تستبدل تغييرات شخص آخر ولا تستخدم force push.
2. حافظ على المصدر في `dist/` و`server/` و`db/`، وعلى هوية Sites الحالية.
3. نفذ `npm test` و`npm run build` قبل رفع التغييرات.
4. ارفع التعديلات التي طلبها المستخدم إلى GitHub بعد اكتمالها والتحقق منها. لا ترفع الأسرار أو ملفات البيئة أو بيانات الحسابات أو أرشيفات النشر.
5. عند تعديل المخطط: `npm run db:generate`، ثم افحص SQL والترحيلات. لا تعدّل ترحيلًا سبق تطبيقه.
6. حفظ الكود على GitHub ليس نشرًا تلقائيًا للتطبيق. لا تدعِ تحديث الرابط دون تأكيد نجاح النشر.
